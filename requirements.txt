# MTAP runtime dependencies
# Install with:  pip install -r requirements.txt
#
# cadquery-ocp  (~1 GB download) provides all OCP.* modules used for
# solid modelling and DXF projection.  Install time: 5-15 min on first run.

PyQt6
numpy
cadquery-ocp
ezdxf
